1. git init
(tworzy folder o nazwie .git)
(tylko pierwszy raz)

2. git remote add origin git@bitbucket.org:projektgrupowy123/projektgrupowy.git

3. git add .
git status

4. git commit -m "Init"

5. git push -u origin master

